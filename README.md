# ethan-codes.com

Fresh start — rebuilding from scratch. Nothing here yet.

Previous versions are preserved in git:

- **`pre-rebuild` tag** — the original portfolio (Alignment Atlas, Dimension
  Visualizer, Snake, calculator).
- **`scrapped-ethanos` branch** — the EthanOS "WebOS" rebuild
  (Vite + TypeScript + GSAP + Three.js).
